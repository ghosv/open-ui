<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no"
    />
  </head>
  <body>
    <div id="root"></div>

    <!-- TODO: test -->
    <!--
    <script type="module">
        import { record, DB, replay } from 'https://oct16.github.io/WebReplay/replay.esm.js'
        DB.then(db => {
            db.clear()
            const ctr = record({
                emitter: data => {
                    db.add(data)
                }
            })
            window.replay = () => {
                ctr.uninstall()
                replay()
            }
        })
    </script>
    -->
  </body>
</html>
